package.json{
  "name": "njbot-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "NJBot Frontend",
  "scripts": {
    "build": "mkdir -p build && cp -r painel-empresas/* build/ && cp *.html build/ 2>/dev/null || true"
  }
}
