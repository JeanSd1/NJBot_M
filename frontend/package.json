package.json{
  "name": "njbot-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "NJBot Frontend",
  "scripts": {
    "build": "ls -la && echo 'Build complete' && mkdir build"  }
}
